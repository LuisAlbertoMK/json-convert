# json-convert environment variables
# Copy this file to .env and fill in your values

# HTTP proxy for corporate networks (optional)
# HTTP_PROXY=http://proxy:8080
# HTTPS_PROXY=http://proxy:8080

# Git credential helper (optional — use instead of --store)
# GIT_CREDENTIAL_HELPER=cache
